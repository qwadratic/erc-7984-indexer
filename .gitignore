# deps
node_modules/

# secrets
.psst/
.env
.env.local

# ponder / data
.ponder/
data/
generated/

# foundry
evm/out/
evm/cache/
evm/broadcast/**/dry-run/
broadcast/

# recordings — raw casts kept, scratch ignored
recordings/*.tmp
recordings/scratch/

# pid lock
.decrypt-worker.lock

# local deploy outputs
scripts/local-addresses.env

# misc
*.log
.DS_Store
cache/

# session exports are auto-regenerated UNREDACTED — never commit (contain secrets)
.env.*.local
*.pem
*.key
id_rsa
id_ed25519

# heavy/local artifacts — video on YouTube, instance live; not tracked
demo/
demos/
docs/
recordings/
graphify-out/

# planning doc — kept local, not in repo
PLAN.md
